Pseudo Code:

//Counts the number of elements in an array that are less than x.
//A[1...N], an array with N elements
count_l = 0

for i = 1 to N
    if A[i] < x
        count_l = count_l + 1


//Counts the number of elements in an array that are less than or equal to x.
//A[1...N], an array with N elements
count_leq = 0

for i = 1 to N
    if A[i] <= x
        count_leq = count_leq + 1


//Counts the number of elements in an array that are greater or equal to x.
//A[1...N], an array with N elements
count_geq = 0

for i = 1 to N
    if A[i] >= x
        count_geq = count_geq + 1


//Counts the number of elements in an array that are greater than a given element (A[x])
//A[1...N], an array with N elements
count_e = 0
element = A[x] //Assigns the value of the element x as a variable

for i = 1 to N
    if A[i] > element
        count = count_e + 1


//Reverses an array
//A[1...N], an array with N elements

for i = 1 to (N / 2)
    tmp = A[i]
    A[i] = A[N + 1 - i]
    A[N + 1 - i] = tmp


//Counts even elements of an array
//A[1...N], an array with N elements
count_even = 0

for i = 1 to N
    if A[i] mod 2 == 0
        count_even = count_even + 1

//Counts odd elements of an array
//A[1...N], an array with N elements
count_odd = 0

for i = 1 to N
    if A[i] mod 2 != 0
        count_odd = count_odd + 1

//Counts number of elements of an array divisible by a certain number x
//A[1...N], an array with N elements
count_x = 0

for i = 1 to N
    if A[i] mod x == 0
        count_odd = count_odd + 1
